# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete this file and re-run
# i3-config-wizard(1).
#

exec xrandr --output HDMI1 --auto --right-of eDP1

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
set $mod Mod4
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font monospace 8 

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#fontpango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default: font
# -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1 The font above is
# very space-efficient, that is, it looks good, sharp and clear in small sizes.
# However, its unicode glyph coverage is limited, the old X core fonts
# rendering does not support right-to-left and this being a bitmap font, it
# doesn’t scale on retina/hidpi displays.
font pango:System San Francisco Display 13 

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# i3-sensible-terminal
exec urxvtd --quiet --opendisplay --fork
bindsym $mod+Return exec urxvtc

# kill focused window
bindsym $mod+Shift+q [con_id="__focused__" class="^(?!Chromium)"] kill
#i3-persist kill

#kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.  bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

## move focused window
#bindsym $mod+Shift+j move left
#bindsym $mod+Shift+k move down 
#bindsym $mod+Shift+l move up
#bindsym $mod+Shift+colon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down 
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later
# on.  We use variables to avoid repeating the names in multiple places.
set $wsBrowserPersonal "1  Browser (Personal)"
set $wsBrowserWork "2  Browser (Work)"
set $wsTerminals "3  Terminals"
set $wsChat "4  Chat"
set $wsMail "5  Mail"
set $wsCode "6  Code"
set $wsDiagrams "7  Diagrams"
set $wsWorkflowy "8  Workflowy"
set $wsUnknown2 "9 Unknown"
set $wsCustom "10 Custom"

# switch to workspace
bindsym $mod+1 workspace $wsBrowserPersonal
bindsym $mod+2 workspace $wsBrowserWork
bindsym $mod+3 workspace $wsTerminals
bindsym $mod+4 workspace $wsChat
bindsym $mod+5 workspace $wsMail
bindsym $mod+6 workspace $wsCode
bindsym $mod+F1 workspace $wsCode
bindsym $mod+7 workspace $wsDiagrams, exec (pgrep luakit > /dev/null || luakit --uri=https://www.lucidchart.com) 
bindsym $mod+F2 workspace $wsDiagrams, exec (pgrep luakit > /dev/null || luakit --uri=https://www.lucidchart.com) 
#bindsym $mod+8 workspace $wsBrowserPrivate, exec (pgrep firefox > /dev/null || firefox --private-window about:blank) 
bindsym $mod+8 workspace $wsWorkflowy, exec workflowy
bindsym $mod+F3 workspace $wsWorkflowy, exec workflowy
#bindsym $mod+9 workspace $wsVideo, exec Popcorn-Time
#bindsym $mod+9 workspace $wsVideo, exec popcorntime 
bindsym $mod+9 workspace $wsUnknown2
bindsym $mod+0 workspace $wsCustom

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $wsBrowserPersonal
bindsym $mod+Shift+2 move container to workspace $wsBrowserWork
bindsym $mod+Shift+3 move container to workspace $wsTerminals
bindsym $mod+Shift+4 move container to workspace $wsChat
bindsym $mod+Shift+5 move container to workspace $wsMail
bindsym $mod+Shift+6 move container to workspace $wsCode
bindsym $mod+Shift+7 move container to workspace $wsDiagrams
bindsym $mod+Shift+8 move container to workspace $wsWorkflowy
bindsym $mod+Shift+9 move container to workspace $wsUnknown2
bindsym $mod+Shift+0 move container to workspace $wsCustom

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# move workspaces between monitors
bindsym $mod+Ctrl+Shift+Right move workspace to output right
bindsym $mod+Ctrl+Shift+Left move workspace to output left

# resize window (you can also use the mouse for that)
mode "resize" {
	# These bindings trigger as soon as you enter the resize mode

	# Pressing left will shrink the window’s width.  Pressing right will
	# grow the window’s width.  Pressing up will shrink the window’s
	# height.  Pressing down will grow the window’s height.
	bindsym j resize shrink width 10 px or 10 ppt
	bindsym k resize grow height 10 px or 10 ppt
	bindsym l resize shrink height 10 px or 10 ppt
	bindsym semicolon resize grow width 10 px or 10 ppt

	# same bindings, but for the arrow keys
	bindsym Left resize grow width 10 px or 10 ppt
	bindsym Down resize grow height 10 px or 10 ppt
	bindsym Up resize shrink height 10 px or 10 ppt
	bindsym Right resize shrink width 10 px or 10 ppt

	# back to normal: Enter or Escape or $mod+r
	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+r mode "default" 
}

bindsym $mod+r mode "resize"

# SHUTDOWN

set $Locker i3lock && sleep 1

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Pause mode "$mode_system"

# CUSTOM SETTINGS
set $bg-color #2f343f 
set $inactive-bg-color #2f343f
set $text-color #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color #E53935

# window colors		border			background		text			# indicator
client.focused		$bg-color		$bg-color		$text-color		#00ff00
client.unfocused	$inactive-bg-color	$inactive-bg-color	$inactive-text-color	#00ff00
client.focused_inactive	$inactive-bg-color	$inactive-bg-color	$inactive-text-color	#00ff00
client.urgent		$urgent-bg-color	$urgent-bg-color	$text-color		#00ff00

# hides borders and indicator hide_edge borders both

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar { 
	status_command i3blocks -c /home/rezgar/.config/i3/i3blocks.conf
	colors 	{
		background		$bg-color		separator		#757575
		#			border			background		text
		focused_workspace	$bg-color		$bg-color		$text-color
		inactive_workspace	$inactive-bg-color	$inactive-bg-color	$inactive-text-color
		urgent_workspace	$urgent-bg-color	$urgent-bg-color	$text-color 
	} 
# Hiding the bar by default, showing on Alt press
#	mode hide
#	hidden_state hide
#	modifier Mod1 
}

bindsym $mod+d exec rofi -show run -lines 3 -eh 2 -i \
			 -color-window "$bg-color" \
			 -theme '/etc/nixos/desktop/i3/rofi.theme' \
			 -font "System San Francisco Display 18"

# bindsym $mod+shift+l exec i3lock
bindsym $mod+Shift+l exec xset dpms force off && i3lock -d --color 000000

# Screen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
bindsym XF86TouchpadToggle exec $HOME/.config/i3/scripts/toggletouchpad.sh # toggle touchpad

# Keyboard layout
exec --no-startup-id "setxkbmap -layout us,ru"
exec --no-startup-id "setxkbmap -option grp:toggle"

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Workspace mappings

# associate applications with default workspaces xproc - detect window class
assign [instance = "personal" class = "Chromium"] $wsBrowserPersonal
assign [instance = "work" class = "Chromium"] $wsBrowserWork
assign [class = "^Code$"] $wsCode
assign [class = "Skype"] $wsChat
assign [class = "Slack"] $wsChat
assign [class = "Mattermost"] $wsChat
assign [class = "Pidgin"] $wsChat
assign [class = "zoom"] $wsChat
assign [title = "Thunderbird"] $wsMail
assign [title = "Mailspring"] $wsMail
assign [class = "Luakit"] $wsDiagrams
#assign [class = "Firefox"] $wsBrowserPrivate
assign [class = "Popcorn-Time"] $wsCustom
assign [title = "Butter"] $wsCustom
assign [class = "WorkFlowy"] $wsWorkflowy

for_window [title="^Crossover$"] move scratchpad
bindsym mod4+i [title="^Crossover$"] scratchpad show

for_window [class="Pidgin" window_role="conversation"] floating enable, resize set 900 700, move absolute position center
for_window [class="Gnome-calculator"] floating enable, resize set 400 300, move absolute position center

for_window [class="^.*"] border pixel 1

# Autorun

exec --no-startup-id while true; do wget -q --spider http://google.com ; if [ $? -ne 0 ]; then sleep 1; else bash -c /home/rezgar/Crossover/Crossover; break; fi; done;
exec nm-applet

#exec flameshot
exec screencloud

exec megasync 

exec /home/rezgar/.config/chromium-crash-cleanup.sh
exec --no-startup-id i3-msg 'workspace $wsBrowserPersonal; exec 4< <(i3-get-conid -c "Chromium"); exec chromium --user-data-dir=~/.config/chromium/personal; exec i3-persist lock $(cat <&4);'
exec --no-startup-id i3-msg 'workspace $wsBrowserWork; exec 4< <(i3-get-conid -c "Chromium"); exec chromium --user-data-dir=~/.config/chromium/work; exec i3-persist lock $(cat <&4);'

exec i3-msg 'workspace $wsTerminals; exec urxvtc'
#exec i3-msg 'workspace $wsTerminals; exec urxvtc'
#exec i3-msg 'workspace $wsTerminals; exec urxvtc'
#exec i3-msg 'workspace $wsTerminals; append_layout /home/rezgar/.config/i3/layouts/terminals.json'

#exec code # I rarely use it so no point launching it on start

exec i3-msg 'workspace $wsChat; exec slack'
exec i3-msg 'workspace $wsChat; exec mattermost-desktop'
exec i3-msg 'workspace $wsChat; exec skypeforlinux'
#exec i3-msg 'workspace $wsChat; exec pidgin'
exec i3-msg 'workspace $wsChat; exec telegram-desktop'
# exec i3-msg 'workspace $wsChat; exec zoom' # Sits Idly and consumes a significant amount of the battery 
exec i3-msg 'workspace $wsChat; append_layout /etc/nixos/desktop/i3/layouts/chat.json' 
#exec i3-msg 'workspace $wsChat;'

#exec i3-msg 'workspace $wsMail; exec thunderbird'
exec i3-msg 'workspace $wsMail; exec mailspring'

#exec i3-msg 'workspace $wsFiles; exec urxvtc -e vifm'
#exec i3-msg 'workspace $wsFiles; exec urxvtc -e ranger'

# Custom Shortcuts

#bindsym --release Ctrl+Print exec flameshot gui
#bindsym --release Ctrl+Print exec screencloud -a
#bindsym --release Print exec screencloud -f
#bindsym --release Alt+Print exec screencloud -w
